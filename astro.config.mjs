import { defineConfig } from 'astro/config';

// https://astro.build/config
export default defineConfig({
  // Replace with your GitHub username and repository name
  site: 'https://renovamen1.github.io',
  base: '/renovamen1.github.io',
});